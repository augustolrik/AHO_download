# Puvlic

- Purpose: Public, download-only GitHub Pages library for student files.
- Open locally: run `node .site/scripts/generate-index.mjs`, then serve `.site-dist` with a local web server.
- Current state: Local Git repository is ready for its first commit; the download-only site, Pages workflow, red Windows folder icon, and `Testfil.txt` are ready.
- Blockers: The public GitHub repository and GitHub Pages deployment still need to be created.
- Next useful step: Publish the repository on GitHub, then enable GitHub Actions under Pages.
